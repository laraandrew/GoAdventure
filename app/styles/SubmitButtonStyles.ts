import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    padding: 15,
    borderRadius: 10,
    alignItems: 'center',
  },
  buttonText: {
    color: '#FCFAFA',  // White color
    fontSize: 16,
    fontWeight: 'bold',
  },
});
